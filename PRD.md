# Quillrun — Product & Engineering Context

Status snapshot as of 2026-09-04. This file exists so any future session (human or agent) can pick up this project cold. Update it as phases land — don't let it drift.

See also `PROCESS_ARCHITECTURE.md` — the runtime companion to this file: process flows, control points, and failure modes, rather than what's built. See also `ROUTING_SPEC.md` — every navigation decision across `apps/web`/`apps/app`: entry gates, post-action destinations, OAuth round trips, and known routing gaps.

## 1. What Quillrun is

Quillrun is a multi-tenant SaaS: an autonomous SEO/GEO content agent that researches, writes, and auto-publishes blog content to a customer's own CMS (WordPress, Webflow, Shopify, or a hosted blog we provide), with almost no manual work.

**Who uses it**: small business owners and marketing-ops people at agencies, managing content for one or several client sites. They're trusting an AI to write and publish under their brand with no review step by default. The product's central design problem is making that trust legible — what the agent is about to do, what it just did, why a piece of content passed or failed quality gates, and a clear, always-visible way to stop it. Think operations/monitoring dashboard (Vercel, Linear, a CI pipeline view), not a marketing site — calm, information-dense, never decorative.

**GEO** = Generative Engine Optimization — optimizing content to be cited/surfaced by AI answer engines (ChatGPT, Perplexity, AI Overviews), not just ranked by classic search. This sits alongside traditional SEO as a quality gate in the generation pipeline.

## 2. Repo & deployment topology

- **Local path**: `C:\Users\acer\Documents\seo-geo-agent`
- **GitHub**: `YukeshDhakal/SEO-Optimizer` (public), branch `master`
- **Base template**: [next-forge](https://github.com/vercel/next-forge) v6 — Next.js + Turborepo monorepo, npm workspaces
- **Vercel team**: `ukeshdhakal11-9217's projects` (hobby plan), 3 linked projects, all tracking `master`:
  - `quillrun-app` (prj_ac000ZNtbOu0BKdfAEeCmhdjMYpA) → `apps/app`, the main dashboard product
  - `quillrun-web` (prj_Js7q7jCm3zeWSSSKHaGu1mVhDzKV) → `apps/web`, marketing site + public tenant blog rewrite
  - `quillrun-api` (prj_zbRDysgqfdbMuWgp4z1UpXvjGNdL) → `apps/api`, cron dispatcher + webhooks
- **Custom domains** (added 2026-08-29): `quillrun.dev` → `quillrun-web`, `app.quillrun.dev` → `quillrun-app`, both properly attached as project domains (auto-track `master` pushes, not manual aliases). `quillrun-api` has no public domain — it's cron/webhooks only, never meant to be visited directly.
- **Database/Auth**: Supabase project `acyauqpeykgrivrajksa` ("YukeshDhakal's Org")
- **Lovable design project**: "Content Autopilot" (workspace `dd60d9b2a04383a7c40a`) — pure UI/UX exploration with mock data, used to pull the "Quillrun Dashboard" visual design (cream/teal palette, IBM Plex fonts, 6-state status pills) back into this production app on 2026-08-28. **Superseded 2026-09-01** by the neobrutalism redesign below — kept here only as history, no longer the live look.
- **Claude Design project**: "Quillrun visuals and animations" (`84a67b79-a869-4402-83ef-3d4d2bad1a67`) — the live design source as of 2026-09-01. `.dc.html` files read via the `DesignSync` MCP tool (`get_file`), never the `/design-sync` skill (that skill pushes a local component library *up* to Claude Design — the opposite direction; do not invoke it for pulling a mock down into code). Files seen so far: `Quillrun Neobrutalism.dc.html` (design-system foundation + full dashboard retheme, commits `cc166b9`/`008a4d0`), `Quillrun Visuals.dc.html` (marketing feature graphics + sign-in panel, `628863e`), `Quillrun API Keys.dc.html` (the `/guardrails/api-keys` page, `5b876dc`). Palette/tokens live in `packages/design-system/styles/globals.css`: thick black borders, hard offset drop-shadows, zero border-radius, flat saturated colors (`--primary` orange `#ff5c00`, `--secondary` blue `#2b44ff`, `--accent` lime `#d6f034`, `--status-warning-bg` yellow `#ffd400`, `--destructive` red `#e8412c`, `--background` cream `#fffcf2`), Archivo Black (`font-display`) + Space Grotesk (`font-sans`). One fixed light palette on purpose — no dark mode.

### ✅ Deploy status — all three live as of 2026-08-29
First successful production deploy achieved for all three projects. What it took, in order:

1. **Missing required env vars** — `NEXT_PUBLIC_APP_URL`/`NEXT_PUBLIC_WEB_URL` are the only two env vars actually required at build time (everything else in this codebase is `.optional()` by design, see §5). Added those plus the non-secret Supabase public config (`NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_URL`) to all three Vercel projects via `vercel env add`, pointed at each project's assigned `*.vercel.app` domain.
2. **Hobby-plan cron cadence** — `apps/api/vercel.json`'s `/cron/dispatch-runs` was `*/5 * * * *`, which fails at deploy time on the Hobby plan (daily cron only). Collapsed to `0 2 * * *` as a stopgap (see `PROCESS_ARCHITECTURE.md` §8 for the tradeoff and how to revisit it).
3. **No framework detected** — `quillrun-app`/`quillrun-api` had `framework: null` on Vercel (monorepo auto-detection gap), so the deploy step looked for a static `public/` output instead of the real Next.js build output. Fixed by adding `"framework": "nextjs"` explicitly to each app's `vercel.json`.
4. **BaseHub schema mismatch, twice, then dropped entirely** — first attempt: a blank BaseHub account has no `_slug`/`_title` fields, needed their official next-forge blueprint forked instead. Second attempt (2026-08-31), after a fresh token: fetched the live schema directly (`basehub build` locally with the real token) and confirmed the connected repo had **zero** `PostsItem`/`LegalPagesItem` types at all — `_agent`/`_agents` root fields instead, an entirely different content model, not a field-level mismatch. Since `packages/cms`'s build script ran `basehub build` on every Vercel deploy (re-fetching and overwriting `basehub-types.d.ts` from whatever the live repo currently has), this was never a one-time fix — it would keep breaking for as long as the token pointed at a mismatched repo. Per user direction ("take over and rebuild what you require"), dropped the live-schema dependency entirely (commit `5907045`): `packages/cms/index.ts` no longer uses `fragmentOn`/`basehub.query`, `legal` content is hardcoded real copy in `apps/web/lib/legal-content.ts` (ported verbatim from the design handoff), `blog` degrades to always-empty pending a real content backend, and the `build` script no longer calls `basehub build` at all.
5. **Vercel's monorepo "skip unaffected projects"** — an env-var-only change or an empty commit doesn't register as a file change for a given app's dependency graph, so Vercel silently skips rebuilding it (`readyState: CANCELED`, `errorLink` points at `vercel.com/docs/monorepos#skipping-unaffected-projects`). Whenever only an env var changed, force a rebuild with a trivial real file touch (this repo's own precedent, see commit `397e3a9`) rather than an empty commit.

6. **A CLI-direct deploy (bypassing git) briefly produced a red herring**: a manual `vercel deploy --prod` from an uncommitted working tree failed with "registers no queue consumer" for Workflow DevKit's `.well-known/workflow/v1/*` routes. This looked like a platform-level Vercel Queues gate, but wasn't — the same code deployed cleanly through the normal git-linked pipeline once pushed. The *actual* blocker on that push was a real test regression: `apps/app/__tests__/sign-in.test.tsx`/`sign-up.test.tsx` broke when those pages became async Server Components (added a `currentUser()` inverse-guard check) — RTL's `render()` can't invoke an async component directly, and `@repo/auth/server`'s `import "server-only"` sentinel throws when hit from a test. Fixed by mocking `currentUser` and calling `await Page(...)` before `render()` (commit `51539ff`). Lesson: always push through git and watch the real deploy, not a CLI shortcut — this repo has no local CI, so `turbo run test` failures only surface at deploy time.
7. **`app.quillrun.dev` needed a real project-domain attachment**, not just `vercel alias set` to one deployment — the latter doesn't track future pushes. Fixed via `vercel domains add app.quillrun.dev quillrun-app` once `quillrun-app` had a clean deploy to attach to.
8. **Pre-existing `DYNAMIC_SERVER_USAGE` production crash on legal/blog pages** (commit `8f85253`) — `legal/[slug]` and `blog/[slug]` were the only two `apps/web` routes using `generateStaticParams` (SSG); every other route, including their own `[locale]` parent, renders dynamically. Mixing static generation into an otherwise fully-dynamic `[locale]`-nested tree threw in production (confirmed via `get_runtime_errors`, first seen 2026-08-30 — predates the BaseHub work, was just masked by the build never succeeding until now). Dropped `generateStaticParams` from both; content is static either way, dynamic per-request rendering costs nothing meaningful here.
9. **Shared page-metadata defaults still said next-forge/Vercel** (commit `3ccd1b2`) — `@repo/seo/metadata.ts`'s `createMetadata()` (used site-wide) had `applicationName = "next-forge"`, author/publisher `"Vercel"`. Every page `<title>` was suffixed `| next-forge`. Rebranded to Quillrun.

**Still open**: `SUPABASE_SERVICE_ROLE_KEY` is not set on Vercel — no tool can read it back out of the Supabase dashboard, so paste it in manually (Project Settings → API) before database writes will work in production. `STRIPE_SECRET_KEY`/`STRIPE_WEBHOOK_SECRET` are also still unset (see §4's Stripe fixes commit `24eff3f` for what's ready once they are — restricted key preferred, mark Sensitive on Vercel). Several open Dependabot PRs (react/@types/react, concurrently, sharp bumps) are still sitting untriaged against `master`. Blog has no real content backend (degrades to empty by design — see item 4). Real Terms/Privacy/AUP text is live but per the design handoff's own note, needs a lawyer's review before real launch.

## 3. Architecture

```
apps/
  app/          main tenant dashboard (sign-in, onboarding, sites, generate, runs, posts, schedule, settings, billing)
  web/          marketing site + public tenant-blog rewrite (org-slug.{ROOT_DOMAIN}/blog/{slug})
  api/          cron dispatcher (/cron/dispatch-runs), Stripe/webhook receivers, JWT verification
  docs/         Mintlify docs (untouched from next-forge scaffold)
  email/        React Email templates (untouched from next-forge scaffold)
  storybook/    component dev environment (untouched)

packages/
  ai-engine/         the generation pipeline: topic_selection → research → outline → draft →
                      geo_seo_optimize → policy_check, on Vercel AI SDK + Anthropic
  workflows/          Workflow DevKit orchestration wrapping ai-engine in durable "use step" functions
  cms-adapters/       CmsAdapter interface + registry; hosted-blog, WordPress, Webflow, Shopify
  search-console/     Google Search Console OAuth2 + Search Analytics client
  google-ads/         Google Ads Keyword Planner OAuth2 + volume lookups
  security/           shared OAuth state-signing helper (used by search-console + google-ads)
  database/           Supabase client (service-role) + generated types + all SQL migrations
  auth/               @supabase/ssr wrapper (browser/server clients, useAuth hook, session-refresh proxy)
  design-system/      shared UI components (shadcn-based), Quillrun brand tokens
  payments/           Stripe (next-forge default, wired up in Phase 6)
  webhooks/           Svix (next-forge default)
  observability, feature-flags, notifications, storage, rate-limit, cms, ai,
  analytics, collaboration, internationalization, next-config, typescript-config
                      — next-forge defaults, mostly unconfigured/optional (see §5)
```

**Stack swaps from stock next-forge** (Phase 0): Clerk → Supabase Auth; Prisma+Neon → Supabase Postgres. Reason recorded in commit `a3f0311`: avoid a second paid auth vendor and keep DB+auth on one platform already in use elsewhere (quoteengine.dev).

## 4. Build history — phase by phase

Each phase is one commit on `master` (chronological, oldest first). This is the authoritative "what's been done":

| Phase | Commit | What shipped |
|---|---|---|
| 0 | `d3d351e`, `a3f0311` | next-forge scaffold; swapped Clerk→Supabase Auth, Prisma/Neon→Supabase DB. New Supabase project provisioned. |
| 1 | `8369677` | Multi-tenant core: `organizations`, `organization_members`, `user_profiles`, `site_connections`, `cms_credentials`. RLS via `is_org_member/admin/owner` security-definer helpers. `/onboarding`, `/sites`, `/sites/[id]`. RLS empirically verified live (outsider rejected, member read-only, owner full access). |
| 2 | `10b7d13` | `packages/cms-adapters`: hosted-blog + WordPress (REST API, Application Passwords). `posts` table. Credentials stored via Supabase Vault, never plaintext — `set_site_credentials`/`get_site_credentials` RPCs. "Publish now" manual flow. Tenant blog rewrite in `apps/web`. |
| 3 | `6b8ef22` | `packages/ai-engine`: full pipeline on Vercel AI SDK + Anthropic. Real `webSearch` tool for research (no invented citations). draft↔geo_seo_optimize retry loop (max 2 retries) as a hard blocker. `pipeline_runs`/`pipeline_run_steps` tables. Manual trigger only (`/sites/[id]/generate`), no durable orchestration yet. 21 unit tests. |
| — | `f492830` | Fixed `.env.example` pattern: unset optional vars instead of `""` (zod `.url()`/`.startsWith()` reject empty string as invalid, not "unset"). |
| 4 | `e5c2a77` | `packages/workflows`: Workflow DevKit durable orchestration (retry/caching per step), preserving Phase 3's retry loop exactly. `schedules` + `tenant_settings` tables. Cron dispatcher in `apps/api` (`CRON_SECRET`-gated), skips paused tenants/sites without backlogging. Real `approval_gate` via `createHook()`/`resumeHook()` suspend-resume. `/sites/[id]/schedule`, `/settings`. |
| 5 | `8460edf` | Guardrails: kill switch (re-checked twice per run, plus platform-wide `EMERGENCY_STOP` env var), `max_posts_per_day/week` enforcement, auto-pause after 3 consecutive publish failures (DB trigger), duplicate-content detection via pgvector cosine similarity (degrades gracefully — needs `OPENAI_API_KEY`, not configured), `audit_log` table + `/settings/audit` page. 31 tests total. |
| 8 (fast-follow) | `7227ceb` | Webflow + Shopify CMS adapters, done early per user request. Shopify via GraphQL Admin API (`articleCreate`; REST is legacy post-Oct 2024). Webflow via Data API v2 (`isDraft:false` live-publishes in one call); no fixed schema, so credentials include a collection ID + field-slug mapping, validated live in `testConnection`. Both connect forms fetch real options rather than asking for raw IDs blind. 24 cms-adapters tests. |
| 6 | `e179dbc` | Stripe billing: org-level plans/subscriptions/usage tracking, billing settings page, checkout/portal actions, webhook wired to the Phase 5 `past_due` guardrail. |
| 7 | `3202f4a` | `packages/search-console`: GSC OAuth2 + Search Analytics client, Vault-backed per-site credentials, daily sync cron caching top queries, topic-selection prompt grounded in that data when present. |
| — | `6e5492e` | Reskin to match the "Quillrun Dashboard" Lovable/Claude Design mockup: cream/teal palette, IBM Plex Sans/Mono, 6-state status-pill system (ok/running/await/blocked/failed/paused) cascaded through `packages/design-system` into both `apps/app` and `apps/web`. |
| — | `bc5270c` | Allowed known-safe native/build install scripts (esbuild, sharp, sentry-cli, swc). |
| 8 | `e4bb409` | `packages/google-ads`: OAuth connect flow, daily keyword-research sync seeded from each site's GSC top queries, 4th pipeline quality gate blocking runs with no real search traction + near-zero keyword volume. OAuth state-signing extracted into shared `packages/security`. |
| — | `3159abd` | Dropped `bun` wrapper from scripts — deploy environment only has npm, `package-lock.json` is what's committed. |
| — | `422641c`, `397e3a9` | Attempts to trigger the first Vercel deploy — **still failing**, see §2. |
| — | `8b3c3aa` | Added `@supabase/server` to `apps/api` for JWT verification via the new Supabase key pair (`SUPABASE_PUBLISHABLE_KEY`/`SUPABASE_SECRET_KEY`/`SUPABASE_JWKS_URL`), alongside the legacy anon/service_role keys `@repo/auth`/`@repo/database` still use. |
| — | `73b6c20`–`2480521` | First successful production deploys (see §2 for the full sequence: env vars, cron cadence, framework detection, BaseHub schema). Stripe best-practices review (`24eff3f`): SDK 20.4.1→22.6.0, `apiVersion` bumped, `STRIPE_SECRET_KEY` validator now accepts restricted (`rk_`) keys, webhook handler covers `invoice.paid` alongside `invoice.payment_succeeded`. |
| — | `d91d293` | Added `ROUTING_SPEC.md` — navigation/redirect map for `apps/web`+`apps/app`. |
| — | `b3bb690`, `51539ff` | Closed several `ROUTING_SPEC.md` gaps: `?next=` return-to on the session gate, sign-in/sign-up inverse guard, OAuth-callback session-expiry preserves the connect target, dropped the prod-only condition on the `/legal` redirect, Turbopack monorepo-root fix. Rebrand cleanup ("Acme Inc"→"Quillrun" across sign-in/sidebar/header), stripped fake next-forge demo sidebar nav, rebuilt the pricing page and `en.json` dictionary with real Quillrun copy (was 100% generic boilerplate, including fake testimonials attributed to next-forge's own maintainers). Root `/` now redirects to `/sites` as a stopgap. Custom domains `quillrun.dev`/`app.quillrun.dev` attached (see §2). |
| — | `5907045`, `8f85253`, `3ccd1b2` | Dropped BaseHub's live-schema dependency entirely (see §2 item 4) — `packages/cms` no longer schema-driven, legal pages use real hardcoded copy from the design handoff, blog degrades to empty. Fixed a pre-existing production `DYNAMIC_SERVER_USAGE` crash on legal/blog pages (§2 item 8). Rebranded shared page-metadata defaults away from next-forge/Vercel (§2 item 9). |
| — | `faaff98`, `f012c1e`, `f8fb3df` | **Dashboard visual rebuild** from the Quillrun Design handoff (`Quillrun Dashboard.dc.html`) against the real Phase 0-8 backend — the big item §7 used to list as deferred. New `StatusPill`/`StatusDot`/`statusGlyph` (design-system) implement the handoff's six-state colour+glyph+shape system, replacing every page's ad hoc `statusVariant()` helper. Dark sidebar + persistent StatusBar (real `tenant_settings.paused`/`require_approval`, one-click stop via new `toggleGlobalPause` action) on every screen. Root `/` is now a real cross-site Overview (stat cards, sites table, "waiting on you"/"needs attention" panels — all live queries, closes the `ROUTING_SPEC.md` gap). Sites, Site detail (new shared `SiteTabs` nav), Generate, Runs, Run detail, Posts, Publish now, Schedule, Settings (new dark `EmergencyStopPanel`), Audit log (real action-taxonomy filters) all restyled with real data throughout — no mock numbers. Verified live against a real account's data (Quote Engine site) after each push, not just build success. |
| — | `cc166b9`, `008a4d0`, `628863e`, `a1469cf` | **Neobrutalism redesign — superseded the cream/teal palette above entirely.** `cc166b9`: design-system foundation (thick borders, hard offset shadows, zero radius, flat saturated colors, Archivo Black + Space Grotesk), marketing site, auth pages, sourced from the Claude Design handoff (see §2). `008a4d0`: retheme-in-place across every dashboard screen plus an IA restructure to match the handoff. `628863e`: marketing feature graphics + sign-in panel from a second handoff file (`Quillrun Visuals.dc.html`). `a1469cf`: dropped remaining next-forge scaffold, rebrand cleanup, README rewrite. This is the live look as of this snapshot — §4's earlier "Dashboard visual rebuild" row (cream/teal) is history only. |
| 9 | `b978788` | SEO recommendation engine + Search Console URL Inspection integration. New `inspectUrl()` in `packages/search-console` (Google's own indexed/crawled view of a page — the real equivalent of "use Google's crawler as a tool", researched against Search Central docs this session). Four pure scoring functions in `packages/workflows` (title/meta rewrite, keyword gap, indexing problem, zero traction) driven by two new cron routes (`sync-url-inspections`, `generate-content-recommendations`, bounded per-run count to respect Google's daily inspection quota). `content_recommendations` upserts by natural key `(site_connection_id, recommendation_type, subject_key)` so a human's dismiss/action survives regeneration, and deletes rows whose underlying condition has resolved. New `/sites/[id]/recommendations` tab. Escalated via an n8n workflow chaining both cron routes on a 4-hour schedule (see §5). |
| — | `5bf9a17` | Fixed a real production bug: `delete_search_console_credentials`/`delete_google_ads_credentials` both called `vault.delete_secret(uuid)`, which has never existed in Supabase Vault's actual function surface (confirmed against `pg_proc`) — every "Disconnect" click for either integration had been silently failing. Fixed to `delete from vault.secrets where id = ...`, verified against an isolated throwaway org/site fixture. Same commit added the first `apps/api/app/internal/*` routes (sites, posts, recommendations, recommendations/dismiss, generate, runs, publish, schedules) — a plain `N8N_INTERNAL_SECRET`-gated JSON API, originally built for an n8n MCP Server Trigger workflow to proxy (see §5's n8n row for how that connects). |
| 10 | `0e54801` | **Customer-facing multi-tenant MCP server** — any future Quillrun customer can connect their own AI client (Claude, Codex, ChatGPT, Cursor, etc.) to Quillrun over MCP, scoped to exactly their own org, distinct from the n8n integration above (which stays the operator's own personal automation layer, single shared secret, not safe to hand to customers). New `api_keys` table (sha-256 `key_hash`, not Vault — one-way comparison only, no need to decrypt back) and `mcp_usage_counters` (per-key monthly call cap + usage tracking, the direct analogue of n8n's own execution counter — see §5). New `apps/api/app/mcp/route.ts` gateway: resolves `organizationId` server-side from the authenticated key hash and never accepts it from the MCP client or tool arguments, then self-calls the existing `/internal/*` routes with that resolved org id. Uses `@modelcontextprotocol/server` (not the `@modelcontextprotocol/sdk` package — that one needs Node's raw `req`/`res`, incompatible with a Next.js Route Handler; `@modelcontextprotocol/server`'s `createMcpHandler` is web-standard `fetch(request)`, confirmed against the shipped `.d.mts`). Small additive change to the 8 internal routes: `x-mcp-source`/`x-mcp-actor` headers let `audit_log` distinguish `"customer_mcp"` (real user attribution) from `"n8n_mcp"` (`actor: null`) without changing n8n's existing behavior (proven via the unchanged half of `internal-routes.test.ts`). New `/guardrails/api-keys` dashboard (list/create/revoke, reveal-once flow — the plaintext key is shown exactly once at creation and never again). |
| 11 | `f2bc0ce`, `67b3a03` | Research knowledge base (§9): `research.ts` grounds fact-extraction in Tavily's raw source `content` instead of the model's own prose summary (Phase A); new `research_chunks` table + `find_similar_research_chunks` RPC let a later run on the same site retrieve real prior findings as additional grounding context via new `fetchResearchContextStep`/`storeResearchChunksStep` (Phase B). Embeddings run locally via Ollama by default (`generateResearchEmbedding`, swappable to OpenAI via `RESEARCH_EMBEDDING_PROVIDER`). Same migration also backfill-documents the pre-existing `posts.content_embedding`/`find_similar_posts` drift (§7) into the committed migration history for the first time. `67b3a03` added the read-only `/sites/[id]/research` dashboard tab. |
| 12 | `65c071b` | Two production fixes found by testing Phase 11 live: (1) `research_chunks` never populated — Vercel functions can't reach local Ollama, so switched production to OpenAI embeddings (`RESEARCH_EMBEDDING_PROVIDER=openai` on both `quillrun-app`/`quillrun-api`), widened `research_chunks.embedding` to `vector(1536)`, `embedding_model` now set explicitly per row instead of via column default. (2) `content-pipeline.ts` had no top-level catch, so a step exhausting its retries left `pipeline_runs.status` stuck at `'running'` forever — added a backstop try/catch; backfilled 6 real stuck rows (5 predating this session, from pre-Gemini-swap Anthropic errors) to `'failed'` with their real captured errors. |
| 13 | `82bdd62`, `41844f6`, `c7da9b1`, `6e2d1f3` | Closes the long-flagged "Live-streaming Generate/Run views" gap (§7, removed). `generatePost` now generates the run's own id and redirects the moment `start()` registers the run, instead of awaiting the full pipeline (`start()` only registers a durable run — only `run.returnValue` waits for completion; the cron dispatcher already proved this fire-and-forget pattern in production). The run-detail page's rendering moved into a new client component, `RunDetailLive`, which subscribes to Supabase Realtime (`pipeline_runs`/`pipeline_run_steps`, newly added to the `supabase_realtime` publication) scoped to that run — the stage timeline and status pill now update live with no reload, including a "Starting your run…" state for the brief window before the row exists. First Realtime usage anywhere in this app. Also: a route-level `loading.tsx` for the whole `(authenticated)` segment (there was none anywhere before this — every navigation was a blank page during its data fetch); real `console.error` logging on every previously-silent embedding-failure catch (found because `research_chunks` stayed empty with zero diagnostic trail); fixed a genuine mislabeling bug found by live testing — a succeeded run showed a green "Published" pill though `finalizeRunSucceeded` only ever creates a `draft` post (actual publishing is `apps/app/app/actions/posts/publish.ts`'s separate action) — relabeled "Draft ready" and de-duplicated the three copies of this status-mapping logic down to one shared source (`runs-table.tsx`). |
| — | `5b876dc`, `56e262f` | UI pass on the Phase 10 dashboard: `5b876dc` restyled `/guardrails/api-keys` to match its own Claude Design handoff file (`Quillrun API Keys.dc.html` — see §2) — status badge colors, hard offset shadows, a read-only banner for members, a richer empty state, an MCP client config snippet in the reveal dialog. `56e262f` moved Billing, Audit log, and API keys out from under a `/guardrails` sub-tab-row (a short-lived intermediate attempt, `e4aec27`, immediately superseded) into the sidebar's own "Workspace" nav group as first-class items alongside Runs/Sites/Topics/Quality gates/Guardrails — matches the handoff's own top-nav intent, adapted to this app's sidebar-only real architecture (see the sidebar's own comment for why). |

**Recurring pattern worth knowing**: every phase that touches RLS gets empirically verified live (outsider/member/admin/owner role simulation via `execute_sql`, fixtures cleaned up after) rather than trusted from policy text alone, and every phase runs a Supabase security-advisor pass afterward — which has caught an over-privileged default RPC grant in nearly every phase (Phase 1, 2, 5 all found and fixed one).

**Content pipeline mechanics genuinely verified live** (2026-09-05, real runs against Quote Engine, not just code review): the `draft`↔`geo_seo_optimize` retry loop actually fires and self-corrects — a real attempt-1 output with `citationCount: 0` (despite 5 real researched sources) was rejected by `validateGeoSeoOutput`, fed back as `feedback`, and attempt 2 came back with `citationCount: 5`; `research`/`outline` each ran exactly once across both attempts while `draft`/`content_guidelines_check`/`site_reference_check`/`geo_seo_optimize` each ran twice, confirming the retry loop is scoped correctly and doesn't re-pay for research per attempt. `approval_gate` suspend/resume verified for real (~4.5 min real suspend, real `resumeHook` resolution, correct pre-finalize kill-switch re-check, `finalizeRunSucceeded` correctly creates a `draft`-status post with `published_at: null` — nothing goes live without the separate "Publish now" action) — both the approve and reject paths, on two different real runs. `checkRateLimit`'s blocking path verified live too: `max_posts_per_day` temporarily set to 1 (already-met), a new run correctly stopped at `rate_limit_check` with `status: 'blocked'` before spending any AI cost (`topic_selection`/`research` never ran), setting reverted immediately after. Guardrail *blocking* branches for `checkKillSwitch` (paused/`EMERGENCY_STOP`), `checkDuplicateContent`, and `keyword_volume_check` remain code-reviewed only, not live-triggered — `EMERGENCY_STOP` in particular halts every tenant's runs platform-wide and shouldn't be flipped without explicit ask.

## 5. Integrations — configured vs. deferred

| Integration | Status | Notes |
|---|---|---|
| Supabase (DB + Auth) | **Configured** | Project `acyauqpeykgrivrajksa`. `SUPABASE_SERVICE_ROLE_KEY` must be pasted in manually from the dashboard — no MCP tool can read it back out. |
| Google Gemini (generation) | Key required, not committed | `GOOGLE_GENERATIVE_AI_API_KEY` — pipeline is inert without it. Swapped from Anthropic/Claude (see `packages/ai-engine/model.ts`) for Gemini's free tier; `gemini-2.5-flash` is the model in use. |
| Tavily (research web search) | Key required, not committed | `TAVILY_API_KEY` — replaces Anthropic's old provider-executed search tool now that generation is on Gemini (see `packages/ai-engine/search.ts`). Optional in the sense every other key here is: research degrades to the model's own knowledge (no live citations) rather than throwing if unset. Free tier: 1,000 searches/month. |
| Google Search Console | OAuth flow built (Phase 7) | Needs `GOOGLE_OAUTH_CLIENT_ID/SECRET`, `GSC_OAUTH_STATE_SECRET`. |
| Google Ads (Keyword Planner) | OAuth flow built (Phase 8) | Same Google Cloud client as GSC + `adwords` scope, plus `GOOGLE_ADS_DEVELOPER_TOKEN` (from an MCC account), `GOOGLE_ADS_LOGIN_CUSTOMER_ID`. |
| Stripe | Built (Phase 6) | `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`. |
| OpenAI embeddings | **Not configured** | `OPENAI_API_KEY` — duplicate-content detection (Phase 5) degrades to skipping the check without it, by design (same posture as every other missing key). Neither Gemini nor Claude serve embeddings here, hence the separate provider. |
| Research knowledge base embeddings (Phase 11/12) | **Configured, live in production** | `RESEARCH_EMBEDDING_PROVIDER="openai"` set on both `quillrun-app` and `quillrun-api` production (2026-09-05) — production reuses the `OPENAI_API_KEY` above. Code still defaults to local Ollama (`llama3.2:1b` + `nomic-embed-text`) when this var is unset, for standalone/offline use only; `research_chunks.embedding` is provisioned at 1536 dimensions (OpenAI's), so local dev against the same shared Supabase project must also set this var to `"openai"` — see §9. |
| Upstash (rate-limit) | **Not configured** | `packages/rate-limit` unused; Phase 5's daily/weekly post caps are enforced via a direct DB count query instead. |
| Resend (email) | Not configured | Optional. |
| BetterStack, PostHog, Sentry, Arcjet, Svix, Liveblocks, BaseHub, Knock, GA | Not configured | All next-forge defaults, left as optional/unset. |
| n8n MCP internal API (`apps/api/app/internal/*`) | **Configured, live** (2026-09-03/04) | `N8N_INTERNAL_SECRET` set on `quillrun-api` production — gates the plain JSON API n8n's own MCP Server Trigger workflow's tool nodes call, distinct from `CRON_SECRET` (different trust boundary: fixed reviewed schedules vs. arbitrary proxied MCP tool calls). This is the operator's personal automation layer — n8n cloud instance `yukwew.app.n8n.cloud`, 14-day trial, single shared bearer token. 11 tools built and functionally verified (real data on reads, correct error relay on negative mutation tests): `list_sites`, `list_posts`, `get_recommendations`, `dismiss_recommendation`, `generate_content`, `get_run_status`, `publish_post`, `list_schedules`, `create_schedule`, `toggle_schedule`, `delete_schedule`. Also runs the Phase 9 recommendation-engine cron chain on a schedule (not exposed as an MCP tool — no scoping/args, would regen every connected site on demand if it were). Real trade-off: MCP access here depends on the n8n trial staying paid; not safe to hand to customers (any holder of the one shared secret can pass any `organizationId`) — see the Phase 10 row below for the customer-facing equivalent. |
| Customer-facing MCP server (`apps/api/app/mcp/*`) | **Configured, live** (Phase 10, 2026-09-04) | `POST /mcp` on `quillrun-api`, per-tenant `api_keys` (dashboard: `/guardrails/api-keys`), `organizationId` always resolved server-side from the key — never client-supplied. Depends on `N8N_INTERNAL_SECRET` also being set (the gateway self-calls the same `/internal/*` routes n8n uses). Client compatibility across Claude Desktop/Codex/ChatGPT/Cursor is flagged but not yet spiked — some expect OAuth rather than a static bearer key (see §7). |

**Env-var gotcha to remember**: this repo's `.env.example` files comment out every unconfigured optional var rather than leaving `KEY=""`, because `@t3-oss/env-nextjs` zod validators (`.url()`, `.startsWith()`) treat empty string as present-but-invalid, not unset — that broke local dev until `f492830` fixed the pattern. Apply the same convention to any new optional var.

## 6. Data model (Supabase, chronological migrations in `packages/database/supabase/migrations/`)

- **Phase 1**: `organizations`, `organization_members`, `user_profiles`, `site_connections`, `cms_credentials` + RLS helper functions
- **Phase 2**: `posts` (metadata-only slice), Vault-backed credential RPCs
- **Phase 3**: `pipeline_runs`, `pipeline_run_steps`
- **Phase 4**: `schedules`, `tenant_settings`, `rejected` run status
- **Phase 5**: `audit_log`; `posts.content_embedding` (pgvector) + `find_similar_posts` RPC; `consecutive_publish_failures` + auto-pause trigger
- **Phase 6**: billing plans/subscriptions/usage tables
- **Phase 7**: Search Console credentials/cache tables
- **Phase 8**: Google Ads credentials/cache tables
- **Phase 9**: `url_inspections` (upsert-on-`post_id`), `content_recommendations` (upsert-by-natural-key, preserves human `status`/`dismissed_at`/`actioned_at` across regeneration)
- **Phase 10**: `api_keys` (sha-256 `key_hash`, not Vault; `monthly_call_limit`, `revoked_at` soft-delete), `mcp_usage_counters` (per-`(api_key_id, period_start)` call counter, mirrors `usage_counters`/`incrementUsage()`'s existing monthly-reset shape from Phase 6)
- **Phase 11**: `research_chunks` (per-site chunked/embedded research, upserted on `(site_connection_id, source_url, chunk_index)`) + `find_similar_research_chunks` RPC; same migration also backfill-commits the pre-existing `posts.content_embedding`/`find_similar_posts` (see §9)
- **Phase 12**: `research_chunks.embedding` widened from `vector(768)` to `vector(1536)` (Ollama's `nomic-embed-text` → OpenAI's `text-embedding-3-small`, table was empty so no data migration needed); `embedding_model`'s column default dropped in favor of the app setting it explicitly per row; `find_similar_research_chunks` dropped and recreated to match the new `p_embedding` dimension

## 7. What's explicitly deferred / not yet built

- Org switcher UI for users belonging to multiple orgs (single-org-per-user works today)
- Invite flow (adding members to an existing org)
- Rich content editor for manual posts (plain textarea today)
- Live integration tests against a real WordPress/Webflow/Shopify site (everything is mocked-fetch — no live site to test against yet)
- Docs/email/storybook apps — untouched next-forge scaffolding, not adapted to Quillrun
- **Sign-in/onboarding pixel-matching to the design handoff.** The dashboard rebuild (§4, commits `faaff98`–`f8fb3df`) covered every screen behind the sidebar (Overview through Audit log); sign-in and onboarding still have their earlier stopgap rebrand (real Quillrun branding/copy, `?next=` handling) rather than the handoff's fuller treatment (dark right-panel with a live "Agent activity" feed).
- Marketing site: the handoff's "How it works" page (6-stage pipeline explainer) and the proof-stats/features/trust-section/FAQ blocks on the home page haven't been built yet — only the hero, pricing, and footer got the full treatment; `packages/internationalization/dictionaries/en.json` still carries this session's earlier interim copy rather than the handoff's exact wording for those sections.
- The handoff's `Quillrun Variations.dc.html` documents alternate treatments considered for the status system, the Generate pipeline view, and the marketing hero (each section names which option is live) — worth a look if any of the shipped choices feel wrong once there's real usage to react to.
- **`audit_log`'s own `create table` is missing from the committed migration history** (confirmed via grep across every migration file — it exists only in the live Supabase project, applied by hand at some point before this repo's migration discipline caught up). A fresh `supabase db reset` would break before reaching Phase 5. Needs a standalone cleanup migration; found during Phase 10, not introduced by it.
- **Phase 10's per-key `monthly_call_limit` bounds total calls but doesn't rate-smooth within a period** — a key could burst its whole month's cap in a minute. Mutating tools are still smoothed by the org-level daily/weekly `checkRateLimit`; read-only tools aren't. Fine for now, worth a sliding-window limiter if it becomes a real problem.
- **MCP client auth compatibility spike, unstarted.** Claude Desktop, Cursor, ChatGPT, and Codex differ in remote-MCP support, and some expect OAuth rather than a static bearer key pasted into config — Phase 10's dashboard assumes the latter works uniformly, unverified against real target clients.
- **Duplicate-content pgvector guardrail (Phase 5) status is uncertain, not confirmed no-op.** `OPENAI_API_KEY` was found set on `quillrun-app` production (2026-09-04, confirmed via `vercel env ls`) — contradicting this file's own earlier claim that it was unset. Whether it's a valid/working key was never live-tested this session (checkDuplicateContent itself wasn't exercised) — verify directly before trusting either claim.
- **A `pipeline_runs` row could get stuck at `status: 'running'` forever if a step exhausted its retries and threw a Workflow DevKit `FatalError`** — `content-pipeline.ts` had no top-level catch between `topic_selection` and the draft-retry loop's own try/catch, so the re-thrown error propagated out of the workflow uncaught and no terminal status was ever recorded. Confirmed live against 6 real stuck runs (5 predating any of this session's changes, back to 2026-08-31 — old Anthropic-key/billing/quota errors from before the Gemini swap; the 6th from a `research` step `generateObject` schema-validation failure). Fixed 2026-09-05: the workflow body now has a backstop try/catch that marks the run `failed` with the real error, matching every other failure path's existing structured-return convention. All 6 stuck rows backfilled to `status: 'failed'` with their real captured errors.

## 9. Research knowledge base — retrieval-grounded research

**The gap**: `research` (`packages/ai-engine/steps/research.ts`) searches the web via Tavily, has the model write free prose about what it found, then extracted structured facts from *that prose* in a second call — never from the real source text Tavily returned (`content` was fetched and discarded, only `{title, url}` survived).

**Phase A — same-run fix, shipped**: fact-extraction now grounds in Tavily's real per-source `content` instead of the model's own prose summary — no new tables, providers, or env vars. Falls back to the model's own prose exactly as before when Tavily is unconfigured.

**Phase B — cross-run reuse, shipped**: source content is chunked and embedded into a new `research_chunks` table (per-site, pgvector-backed) so a later run researching a similar topic on the same site retrieves real prior findings as *additional* grounding context via `fetchResearchContextStep`/`storeResearchChunksStep` (`packages/workflows/ai-steps.ts`). This augments a run's research — it does not skip Tavily or the model call; a true "skip search, we already know this" cache-hit mode would trade in stale facts and no fresh citations, and is deliberately not what this builds.

**Model/resource strategy**: embeddings run through a swappable provider — `generateResearchEmbedding`/`getResearchEmbeddingModel` (`packages/ai-engine/embedding.ts`), config-switched via `RESEARCH_EMBEDDING_PROVIDER=ollama|openai` + `OLLAMA_BASE_URL`, not a hardcoded dependency. **Local Ollama by default in code** (`llama3.2:1b` + `nomic-embed-text`, free, no API key) for standalone/offline use; **production runs OpenAI** (`RESEARCH_EMBEDDING_PROVIDER=openai` set on both `quillrun-app` and `quillrun-api`, 2026-09-05) since Vercel's serverless functions can't reach a laptop's `localhost:11434` — confirmed live, `research_chunks` had 0 rows after real production runs until this switch. `research_chunks.embedding` is provisioned at **1536 dimensions** (OpenAI's `text-embedding-3-small`) as of the same date — a developer running this app locally against the same shared Supabase project must also set `RESEARCH_EMBEDDING_PROVIDER=openai` (Ollama's 768-dim output no longer matches the live column) or every insert/query fails silently (best-effort/swallowed by design, not a crash). `embedding_model` is recorded explicitly per row (not a column default) so it can never mislabel a row if the provider changes again.

**Further options this opens up, not yet built:**
- **Un-stalling the Phase 5 duplicate-content guardrail.** `checkDuplicateContent` (§7) has been a no-op since Phase 5 because `OPENAI_API_KEY` was never set. The same local-Ollama-first, swappable-provider pattern built here could give `posts.content_embedding` a local path too, closing a gap flagged since Phase 5 without waiting on a paid key.
- **Local model for dev/self-hosted generation.** `packages/ai-engine/model.ts`'s `getModel()` already has an explicit unused seam for "a future per-org/BYO-key swap." Ollama's OpenAI-compatible chat endpoint could plug into that seam for local development (no Gemini free-tier rate limits while iterating) or, longer-term, a genuinely self-hosted/BYO-model tier for privacy-sensitive customers.
- **A site's own back-catalog as grounding, not just competitor search results.** The same `research_chunks` mechanism generalizes to indexing a site's own past posts — retrieval-grounded internal linking, or steering topic selection away from angles already covered, beyond today's pure duplicate-content *blocking*.
- **Stronger GEO citation grounding.** GEO is a core product pillar (§1); grounding facts in real retrieved text rather than model paraphrase directly strengthens the `citationCount`/fact-accuracy checks `geo_seo_optimize` already gates on.
- **An MCP tool for it.** Phase 10 already exposes internal routes as customer-facing MCP tools. "Search this site's research knowledge base" or "check a draft for duplication" are natural additions once there's a dashboard-facing use for them.

**Known drift, backfill-documented not fully cleaned up**: this phase's migration also formally documents that `posts.content_embedding` and `find_similar_posts` (Phase 5) were never actually committed to the migration history despite being live in production since Phase 5 — the same undocumented-drift pattern already flagged for `audit_log` above. The migration's `find_similar_posts` definition was copied verbatim from `pg_get_functiondef` run against the live project (2026-09-04), not guessed from `types.ts`'s alphabetized `Args`. No vector index was added on either `posts.content_embedding` or `research_chunks.embedding` — both are exact/sequential scans for now, appropriate at this project's current row counts; a follow-up migration should add an `hnsw` index (pgvector 0.8.2, already live, supports it) once row counts justify it.

**Caught by the post-migration security-advisor pass** (same discipline as every phase, see "Recurring pattern worth knowing" above): `find_similar_research_chunks` was initially granted to `authenticated`, but the function trusts the caller-supplied `p_site_connection_id` with no internal `auth.uid()` check (unlike this repo's `get_site_credentials`-style RPCs) — as a `SECURITY DEFINER` function bypassing RLS, that grant would have let any signed-in user of any org read another org's research content directly via `/rest/v1/rpc`. Fixed same-session (`20260905091500_phase11_lock_down_find_similar_research_chunks_grant.sql`) — now only `postgres`/`service_role` can call it, matching `find_similar_posts`'s own (safer) precedent.

**A read-only dashboard page ships alongside**: `/sites/[id]/research` (new "Research" tab, `apps/app/app/(authenticated)/sites/[id]/research/page.tsx`) lists each site's indexed sources — chunk count, embedding model, first-indexed date, and a text preview — grouped from the raw `research_chunks` rows. RLS-respecting (`@repo/auth/server`'s session client, same ownership-check pattern as `/recommendations`), no mutations, mirrors the Phase 9 recommendations tab's exact card/list styling.

## 10. How to resume work here

1. Check `git log --oneline` against this table — if new commits exist beyond `56e262f`, this file is stale; update §4 first.
2. All three Vercel projects are live (§2) — check deploy status via the Vercel MCP tools if something feels stale, not by assuming.
3. Local dev: `npm run dev` (turbo dev across all apps). `apps/app` on :3000, `apps/web` on :3001.
4. DB migrations: `npm run db:push` (runs `supabase db push` from `packages/database`) — note the `audit_log` gap in §7 before trusting a fresh `db reset`.
5. Tests: `npm test` (turbo test) — 44+ new tests added in Phase 10 alone (`packages/workflows/__tests__/api-keys.test.ts`, `apps/api/__tests__/mcp-route.test.ts`), on top of everything from earlier phases.
