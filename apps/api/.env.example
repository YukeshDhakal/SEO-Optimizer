# Server
# SUPABASE_SERVICE_ROLE_KEY: paste from the Supabase dashboard
# (Project Settings > API) — not exposed by any MCP tool.
SUPABASE_URL="https://acyauqpeykgrivrajksa.supabase.co"
SUPABASE_SERVICE_ROLE_KEY=""
# @supabase/server: new publishable/secret key pair + JWKS verification.
# Paste SUPABASE_SECRET_KEY from the Supabase dashboard (API Keys settings)
# — not exposed by any MCP tool.
SUPABASE_PUBLISHABLE_KEY="sb_publishable_wX422O_TKniydizTWpvC7g_0jayQq7a"
SUPABASE_SECRET_KEY=""
SUPABASE_JWKS_URL="https://acyauqpeykgrivrajksa.supabase.co/auth/v1/.well-known/jwks.json"
# RESEND_FROM=""  # leave unset (not empty-string) - optional zod url/format validators reject "" as invalid, fill in and uncomment to enable
# RESEND_TOKEN=""  # leave unset (not empty-string) - optional zod url/format validators reject "" as invalid, fill in and uncomment to enable
# STRIPE_SECRET_KEY=""  # leave unset (not empty-string) - optional zod url/format validators reject "" as invalid, fill in and uncomment to enable
# STRIPE_WEBHOOK_SECRET=""  # leave unset (not empty-string) - optional zod url/format validators reject "" as invalid, fill in and uncomment to enable
# Phase 7: same Google OAuth Client as apps/app/.env.example — needed here
# too since /cron/sync-search-console refreshes expired access tokens.
# GOOGLE_OAUTH_CLIENT_ID=""  # leave unset (not empty-string) - optional zod url/format validators reject "" as invalid, fill in and uncomment to enable
# GOOGLE_OAUTH_CLIENT_SECRET=""  # leave unset (not empty-string) - optional zod url/format validators reject "" as invalid, fill in and uncomment to enable
# Phase 8: same Google Ads credentials as apps/app/.env.example — needed here
# too since /cron/sync-keyword-research refreshes expired access tokens.
# GOOGLE_ADS_OAUTH_STATE_SECRET=""  # any long random string, distinct from GSC_OAUTH_STATE_SECRET
# GOOGLE_ADS_DEVELOPER_TOKEN=""  # leave unset (not empty-string) - optional zod url/format validators reject "" as invalid, fill in and uncomment to enable
# GOOGLE_ADS_LOGIN_CUSTOMER_ID=""  # the MCC account's customer id
# GOOGLE_ADS_DEFAULT_GEO_TARGET_CONSTANT=""  # optional, defaults to US if unset
# GOOGLE_ADS_DEFAULT_LANGUAGE_CONSTANT=""  # optional, defaults to English if unset
# BETTERSTACK_API_KEY=""  # leave unset (not empty-string) - optional zod url/format validators reject "" as invalid, fill in and uncomment to enable
# BETTERSTACK_URL=""  # leave unset (not empty-string) - optional zod url/format validators reject "" as invalid, fill in and uncomment to enable
# FLAGS_SECRET=""  # leave unset (not empty-string) - optional zod url/format validators reject "" as invalid, fill in and uncomment to enable
# ARCJET_KEY=""  # leave unset (not empty-string) - optional zod url/format validators reject "" as invalid, fill in and uncomment to enable
# SVIX_TOKEN=""  # leave unset (not empty-string) - optional zod url/format validators reject "" as invalid, fill in and uncomment to enable
# LIVEBLOCKS_SECRET=""  # leave unset (not empty-string) - optional zod url/format validators reject "" as invalid, fill in and uncomment to enable
# BASEHUB_TOKEN=""  # leave unset (not empty-string) - optional zod url/format validators reject "" as invalid, fill in and uncomment to enable
VERCEL_PROJECT_PRODUCTION_URL="http://localhost:3002"
# KNOCK_API_KEY=""  # leave unset (not empty-string) - optional zod url/format validators reject "" as invalid, fill in and uncomment to enable
# KNOCK_FEED_CHANNEL_ID=""  # leave unset (not empty-string) - optional zod url/format validators reject "" as invalid, fill in and uncomment to enable

# Client
NEXT_PUBLIC_SUPABASE_URL="https://acyauqpeykgrivrajksa.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="sb_publishable_wX422O_TKniydizTWpvC7g_0jayQq7a"
# NEXT_PUBLIC_GA_MEASUREMENT_ID=""  # leave unset (not empty-string) - optional zod url/format validators reject "" as invalid, fill in and uncomment to enable
# NEXT_PUBLIC_POSTHOG_KEY=""  # leave unset (not empty-string) - optional zod url/format validators reject "" as invalid, fill in and uncomment to enable
# NEXT_PUBLIC_POSTHOG_HOST=""  # leave unset (not empty-string) - optional zod url/format validators reject "" as invalid, fill in and uncomment to enable
NEXT_PUBLIC_DOCS_URL="http://localhost:3004"
NEXT_PUBLIC_APP_URL="http://localhost:3000"
NEXT_PUBLIC_WEB_URL="http://localhost:3001"

# Phase 5 platform-wide safety valve - leave commented (unset) by
# default, not "false": z.enum(["true","false"]) rejects "" the same way
# the .url()/.startsWith() validators above do.
# EMERGENCY_STOP="false"
